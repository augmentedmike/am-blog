{
  "version": 2,
  "buildCommand": "echo 'static site — no build needed'",
  "outputDirectory": "docs",
  "rewrites": [
    { "source": "/api/react", "destination": "/api/react.js" }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "X-Frame-Options", "value": "DENY" }
      ]
    }
  ]
}
